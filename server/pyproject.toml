[project]
name = "grand-line-coder-server"
version = "1.0.0"
description = "Gamified DSA/System Design mastery platform - Backend"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
